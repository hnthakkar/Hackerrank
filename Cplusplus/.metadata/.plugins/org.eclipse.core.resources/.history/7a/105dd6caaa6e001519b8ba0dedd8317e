#include <bits/stdc++.h>
using namespace std;

int main() {
	while (true) {
		int N;
		cin >> N;
		if (N == 0)
			break;
		int input[N];
		for (int i = 0; i < N; i++) {
			cin >> input[i];
		}
		sort(i);
		int count = 0;
		for (int i = 0; i < N; i++) {
						}
		}
		cout << count << endl;
	}
	return 0;
}
